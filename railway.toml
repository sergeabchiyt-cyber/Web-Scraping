[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && scrapling install"

[deploy]
startCommand = "python command_center.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
